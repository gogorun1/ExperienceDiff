{
  "name": "@experience-diff/narrator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node dist/index.js",
    "test": "tsc --build && node dist/verify.js"
  },
  "dependencies": {
    "@experience-diff/contract": "*",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
